const fs = require("fs"); const path = require("path"); const filePath = "cursor_0.47.7_app/out/vs/workbench/workbench.desktop.main.js"; try { const stats = fs.statSync(filePath); const fileSizeInBytes = stats.size; const fileSizeInMB = fileSizeInBytes / (1024 * 1024); console.log(`文件路径: ${path.resolve(filePath)}`); console.log(`文件大小: ${fileSizeInBytes} 字节`); console.log(`文件大小: ${fileSizeInMB.toFixed(2)} MB`); const content = fs.readFileSync(filePath, "utf8"); const lineCount = content.split("\n").length; console.log(`行数: ${lineCount}`); } catch (err) { console.error("分析文件时出错:", err); }
